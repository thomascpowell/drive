services:
  backend:
    build:
      context: ./backend
    volumes:
      - db_data:/app/data
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
    image: frontend
    restart: "no"

  nginx:
    build:
      context: ./nginx
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - frontend 
      - backend
    restart: unless-stopped

volumes:
  db_data:
