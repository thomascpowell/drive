let API_URL = "/api"
// if (typeof window !== "undefined") {
//   const { hostname, port } = window.location
//   API_URL = (hostname === "localhost" && port === "5173") ? "http://localhost:8080" : "/api"
// }
export { API_URL }
